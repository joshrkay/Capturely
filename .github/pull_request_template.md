## Summary

-

## Gate A Acceptance Checklist

- [ ] Local Postgres integration run completed (`docker compose up -d` + `npx prisma migrate dev`)
- [ ] Migration files committed under `prisma/migrations`
- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] `/api/health/db` verified

## Evidence

- Commands run:
- Output snippets:
- Screenshots (if applicable):

## Risk / Rollback

- Risk:
- Rollback plan:
